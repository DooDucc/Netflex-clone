import { Navbar, Lists, HeroBanner } from '../components';
const Home = () => {
    return (
        <div>
            <Navbar />
            <HeroBanner />
            <Lists />
        </div>
    );
};

export default Home;
